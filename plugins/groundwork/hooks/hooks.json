{
  "description": "Groundwork transcript harvest. Off by default: both hooks exit immediately unless capture has been enabled for the project with /groundwork-metrics on. When enabled, they copy Claude Code subagent transcripts out of the per-user temp root, where they are deleted within roughly three days, into a local store.",
  "hooks": {
    "SubagentStop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/harvest-transcripts\" --event subagent-stop",
            "timeout": 30
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/harvest-transcripts\" --event session-start",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
